unit View.Frame.Recheio;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants, 
  FMX.Types, FMX.Graphics, FMX.Controls, FMX.Forms, FMX.Dialogs, FMX.StdCtrls,
  FMX.Objects;

type
  TFormRecheio = class(TFrame)
    imgRecheio: TImage;
    procedure imgRecheioClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

implementation

uses
  Pizza.View.Main;

{$R *.fmx}

procedure TFormRecheio.imgRecheioClick(Sender: TObject);
begin
  FormPizza.FloatAnimation3.Stop;
end;

end.
